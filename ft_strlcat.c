#include "libft.h"

size_t  ft_strlcat(char *dst, const char *src, size_t size)
{
    size_t i;
    size_t j;
    i = 0;
    j = 0;
    while(src[i] && i < )
    return(ft_strlen(dst) + ft_strlen(src));
}